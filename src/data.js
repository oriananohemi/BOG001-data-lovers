
export const filterData = (data, condition) => {
  data.filter(dato => {
    dato.type === condition})
  return 'example';
};

// export const sortData = (data, sortBy, sortOrder) => {
//   return 'OMG';
// };